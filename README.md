# TaskFlow

TaskFlow — это **многопользовательский** менеджер задач с консольным и HTTP интерфейсом, поддерживающий создание, редактирование, удаление задач с изоляцией данных между пользователями.

Проект написан на C++17/20 с использованием:

- **core** — логика задач и менеджер задач
- **server** — HTTP сервер на cpp-httplib и nlohmann/json
- **client** — консольное приложение
- **tests** — модульные тесты на GoogleTest

---

## 🆕 Мультипользовательские возможности

- **Изоляция данных** — каждый пользователь видит только свои задачи
- **Простая авторизация** — вход по имени пользователя
- **Раздельное хранение** — задачи сохраняются в `data/<username>_tasks.json`
- **Смена пользователя** — можно сменить пользователя без перезапуска приложения

---

## 📁 Структура проекта

```
TaskFlow/
├── core/           # Логика приложения
├── server/         # HTTP сервер
├── client/         # Консольное приложение
├── tests/          # Тесты GoogleTest
├── data/           # Данные пользователей
│   ├── user1_tasks.json
│   └── user2_tasks.json
└── CMakeLists.txt  # Главный CMake
```

---

## 🛠 Требования

- C++ компилятор с поддержкой C++17/20 (GCC, Clang, MSVC)
- CMake >= 3.16
- Ninja или другой генератор сборки (опционально)
- Git (для скачивания зависимостей через FetchContent)

---

## 🔧 Сборка проекта

1. **Создать папку сборки:**

```bash
mkdir build
cd build
```

2. **Настроить проект через CMake:**

```bash
cmake -S .. -B . -G "Ninja" -DCMAKE_BUILD_TYPE=Release
```

> Замените `"Ninja"` на нужный генератор (`"Unix Makefiles"` или Visual Studio), если используете другую сборку.

3. **Собрать проект:**

```bash
cmake --build .
```

---

## 👤 Запуск системы

### 1. Запустите сервер:
```bash
./server/server
```
Сервер запустится на `http://localhost:8080`

### 2. Запустите клиент:
```bash
./client/task_tracker
```

### 3. Авторизация:
При запуске клиента потребуется ввести имя пользователя:
```
=== Добро пожаловать в Task Tracker! ===
Введите ваш никнейм: 
```

---

## 📱 Меню приложения:

1. Создать задачу
2. Показать все задачи
3. Редактировать задачу
4. Изменить статус задачи
5. Удалить задачу
6. Показать просроченные задачи
7. Выйти (сменить пользователя)
8. Завершить программу

---

## 🌐 HTTP API сервера

Сервер слушает на `http://localhost:8080`.

### Основные маршруты:

* **POST /login** — вход пользователя
* **POST /tasks/list** — список задач пользователя
* **POST /tasks** — создать задачу
* **PUT /tasks** — редактировать задачу  
* **DELETE /tasks** — удалить задачу
* **PUT /tasks/toggle** — изменить статус задачи
* **POST /tasks/overdue** — просроченные задачи

**Все запросы требуют параметр `username` в теле запроса.**

Пример создания задачи:
```json
{
  "username": "alice",
  "title": "Новая задача",
  "description": "Описание задачи",
  "deadline": "2025-12-31 18:00"
}
```

---

## 🧪 Запуск тестов

Тесты находятся в `tests/run_tests`.

1. После сборки выполнить:

```bash
ctest --output-on-failure
```

2. Или запустить напрямую:

```bash
./tests/run_tests      # Linux / macOS
.\tests\run_tests.exe  # Windows
```

---

## 💾 Хранение данных

- Каждый пользователь имеет свой файл: `data/<username>_tasks.json`
- Формат даты: `YYYY-MM-DD HH:MM`
- Пустой дедлайн = нет срока выполнения
- Автоматическое сохранение после каждого изменения

Пример структуры файла:
```json
[
  {
    "id": 1,
    "title": "Задача 1",
    "description": "Описание",
    "deadline": "2025-12-31 18:00",
    "completed": false
  }
]
```

---

## 📝 Примеры использования

### Смена пользователя:
```
Меню пользователя: alice
Выберите пункт: 7
=== Добро пожаловать в Task Tracker! ===
Введите ваш никнейм: bob
Привет, bob!
```

### Просмотр просроченных задач:
```
=== Просроченные задачи (2) ===
ID: 1 | Сделать отчет | Дедлайн был: 2025-01-15 10:00
ID: 3 | Оплатить счета | Дедлайн был: 2025-01-10 12:00
```

---

## 🚀 Быстрый старт

### Linux/macOS:
```bash
mkdir build && cd build
cmake .. -G "Ninja" -DCMAKE_BUILD_TYPE=Release
cmake --build .
./server/server &       # Запустить сервер в фоне
./client/task_tracker   # Запустить клиент
```

### Windows (PowerShell):
```powershell
mkdir build
cd build
cmake .. -G "MinGW Makefiles"
cmake --build .
.\server\server.exe
.\client\task_tracker.exe
```

---

## 📄 Полезные заметки

- Сначала всегда запускайте сервер!
- Для работы с разными пользователями не требуется перезапуск
- Формат дедлайна строгий: `ГГГГ-ММ-ДД ЧЧ:ММ`
- Тесты проверяют изоляцию данных между пользователями
- GoogleTest автоматически скачивается через FetchContent

---

**TaskFlow** — простой и эффективный менеджер задач для командной работы с полной изоляцией данных пользователей.